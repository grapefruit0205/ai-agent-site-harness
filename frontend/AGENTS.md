# Frontend instructions

- Install dependencies with `npm ci`.
- Run `npm run lint` and `npm test` before reporting completion.
- Preserve the static export used by the private S3 and CloudFront deployment.
- Keep keyboard navigation, reduced-motion behavior, Korean line breaking, and mobile layout covered by tests.
- Do not describe the D1 and Worker scaffold as a production backend unless deployment evidence proves it.
